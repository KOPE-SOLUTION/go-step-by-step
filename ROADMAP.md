# แผนหลักสูตร 5 Phase — Go Basic → API → Database → Gateway

สถานะ: Phase 1 มีบทเรียนครบ EP1–28 ส่วน Phase 2–5 ยังเป็นแผน เลือกเรียนตามลำดับและทบทวนได้ตามจังหวะของตนเอง

## เส้นทางการเรียนรู้

| Phase | ขอบเขต | ผลลัพธ์ปลายเฟส | สถานะบทเรียน |
|---|---|---|---|
| 1 — Basic | พื้นฐานภาษา กลุ่มบทเดิม 01–08 และ Console mini project | อธิบายและเขียนโปรแกรมเล็กได้เอง | พร้อมเรียน EP1–28 |
| 2 — Practical Go | configuration, JSON, ไฟล์, HTTP client, งานพร้อมกัน กลุ่มเดิม 09–13 | โปรแกรมที่จัดการข้อมูลและ error เป็นระบบ | แผน |
| 3 — Back-end & API | HTTP server, JSON, validation, CRUD ในหน่วยความจำ, test | API บน localhost | เพิ่มเป็นเฟสชัดเจน ยังเป็นแผน |
| 4 — Database | SQL, database/sql, SQLite, migration, transaction, event ID | API ที่ข้อมูลยังอยู่หลัง restart | เพิ่มเป็นเฟสชัดเจน ยังเป็นแผน |
| 5 — IoT / Edge Gateway | อุปกรณ์จำลอง, MQTT, queue, offline, ACK กลุ่มเดิม 14–20 | ส่งข้อมูลย้อนหลังและทดสอบความขัดข้อง | แผน |

เริ่มที่ [Playlist 1 — Go Basic](docs/playlist-01-go-basic/README.md) รายละเอียด Phase 2–5 อยู่ใน [Future Roadmap](docs/FUTURE_ROADMAP.md) เลขกลุ่มบทเดิมเก็บไว้เพื่ออ้างอิง ไม่ได้หมายความให้ข้าม Phase 3–4 ไปเรียน Gateway และเลข EP ใหม่ของเฟสอนาคตยังไม่กำหนดจนกว่าจะเริ่มจัดบทจริง

แผนเดิมมี HTTP client และกล่าวถึงฐานข้อมูลในเรื่อง ACK แต่ยังไม่มีชุดสอนสร้าง Back-end/API และใช้ฐานข้อมูลโดยตรง จึงเพิ่ม Phase 3–4 ก่อน Gateway เพื่อไม่ต้องเรียน server, SQL, transaction และ MQTT พร้อมกัน

## Phase 1: Basic — EP1–28

เตรียมเป็นตอนเล็กครบแล้ว ดู [สารบัญทุก EP](docs/playlist-01-go-basic/README.md) สำหรับลิงก์บทเรียน แบบฝึกหัด และเฉลย

| กลุ่มเดิม | EP ที่ใช้เรียน | สิ่งที่ได้ทำ | นำไปต่อยอด |
|---|---|---|---|
| 01 Hello Go | 1–4 | รัน อ่านโค้ด พิมพ์ข้อความ และอ่าน error | ตรวจเครื่องมือและไล่ปัญหา |
| 02 ตัวแปรและชนิดข้อมูล | 5–9 | ตัวแปร ชนิด ค่าศูนย์ รูปแบบผล คำนวณ | ข้อมูลการวัดและหน่วย |
| 03 เงื่อนไขและลูป | 10–11 | ตรวจเกณฑ์และทำซ้ำ | ประมวลผลข้อมูลหลายรายการ |
| 04 ฟังก์ชันและ error | 12–14 | รับค่า คืนค่า ตรวจ error | แยกหน้าที่และปฏิเสธข้อมูลเสีย |
| 05 array, slice, map | 15–17 | กลุ่มข้อมูลและค้นหาด้วยชื่อ | รายการและสถานะอุปกรณ์ |
| 06 struct, method, pointer | 18–20 | แบบข้อมูล พฤติกรรม และการแก้ต้นฉบับ | ข้อมูลการวัดที่มีความหมาย |
| 07 package และ module | 21–22 | แยกไฟล์ อ่าน import และ go.mod | โปรแกรมที่โตขึ้น |
| 08 unit test | 23–24 | test แรกและค่าขอบเขต | ตรวจเมื่อแก้โค้ด |
| โปรเจกต์สรุป | 25–28 | รายงานค่าจำลอง ตรวจข้อมูล ทำต่อหลังพบรายการเสีย และ test | Application Core ใน Phase 2 |

เตรียมหลาย EP แต่เรียนทีละ EP ได้ การแยก input/output ของฟังก์ชัน และการแยกงานโปรเจกต์ 4 ขั้น ช่วยไม่ให้มีหลายแนวคิดใหม่ในคราวเดียว ทุกขั้นใช้ข้อมูลจำลองในหน่วยความจำ ไม่ใช้เครือข่ายหรือฐานข้อมูล

## Phase 2: Practical Go — โปรแกรมที่ใช้งานได้จริง

| บท | เป้าหมายและงานลงมือทำ | ตรวจว่าทำได้ | นำไปต่อยอด |
|---|---|---|---|
| 09 configuration และ JSON | อ่านรายการอุปกรณ์จำลองจากไฟล์ | JSON เสีย ช่องข้อมูลหาย และค่าที่ใช้ไม่ได้ | เปลี่ยนรายการอุปกรณ์โดยไม่แก้โค้ด |
| 10 ไฟล์และ log | เขียน/อ่านบันทึก ปิดไฟล์ ตรวจ error | ไฟล์หาย สิทธิ์เขียนไม่พอ ข้อมูลอ่านกลับตรงกัน | พื้นฐานคิวบนดิสก์และการตามปัญหา |
| 11 HTTP และ timeout | เรียก server จำลองที่ localhost | server ช้า คืนสถานะผิด และปิดอยู่ | จำกัดเวลารอแหล่งข้อมูล |
| 12 goroutine, channel, context | อธิบายแต่ละศัพท์ก่อนแบ่งงาน รับผล และยกเลิก | งานถูกยกเลิกได้ ไม่มีการรอไม่สิ้นสุด | อ่านหลายอุปกรณ์พร้อมกัน |
| 13 graceful shutdown และงานพร้อมกัน | หยุดรับงาน รอส่วนที่ต้องจบ ปิดทรัพยากร | ทดสอบการหยุดและตรวจ data race เมื่อเครื่องรองรับ | ปิด gateway อย่างมีลำดับ |

ก่อนบท 13 จะพบ **interface**: ข้อตกลงว่าค่าหนึ่งเรียก method อะไรได้ ใช้สลับงานจริงกับตัวจำลองในการทดสอบ โดยมีพื้นฐาน method จาก Phase 1

## Phase 3: Back-end และ REST API

เปิด server ตอบข้อความหนึ่งเส้นทาง → JSON หนึ่งรายการ → ใช้กฎจาก Phase 2 → เพิ่มข้อมูลในหน่วยความจำ → ตรวจ request และตอบ error → CRUD แยกทีละงาน → ทดสอบ handler และหยุด server เริ่มด้วย net/http ไม่บังคับ framework ยังไม่ใช้ฐานข้อมูลใน API แรก ดูขอบเขตและเกณฑ์ตรวจใน [Future Roadmap](docs/FUTURE_ROADMAP.md)

## Phase 4: Database และการเก็บข้อมูลถาวร

รู้จักตาราง/SQL → เขียนและอ่านหนึ่งรายการ → Go database/sql กับ driver → ต่อ API ให้ข้อมูลอยู่หลัง restart → migration → transaction → ป้องกัน event ซ้ำ → ค้นประวัติและสำรองข้อมูล เริ่มวางแผนด้วย SQLite, PostgreSQL เป็นทางเลือกภายหลัง ก่อนลงมือจะตรวจ driver และเครื่องมือกับ Go รุ่นจริงอีกครั้ง รายละเอียดใน [Future Roadmap](docs/FUTURE_ROADMAP.md)

## Phase 5: IoT / Edge Gateway จำลอง

| บท | เป้าหมายและงานลงมือทำ | ตรวจว่าทำได้ | นำไปต่อยอด |
|---|---|---|---|
| 14 อุปกรณ์จำลอง | สร้าง Measurement จากค่าที่ควบคุมได้ กำหนด event ID และเวลาอ่านเดิม | ค่าและเวลาตรวจซ้ำได้ ID แยกเหตุการณ์ได้ | ข้อมูลชุดเดียวกันตลอดการส่งและ retry |
| 15 หลายอุปกรณ์ | timeout แยกอุปกรณ์ รวบรวมผลที่สำเร็จ | ตัวหนึ่งล้มแล้วตัวอื่นยังให้ผล | แยกผลกระทบของความเสียหาย |
| 16 MQTT บนเครื่องตัวเอง | อธิบาย broker, topic, publish, subscribe และ QoS ก่อนใช้ | ส่งข้อมูลจำลองถึง subscriber บน loopback | ช่องทางส่งข้อมูลที่มีผู้รับ |
| 17 บันทึกก่อนส่ง | append ลงคิวบนดิสก์ก่อนส่ง อธิบายจุดที่ถือว่ารับข้อมูลแล้ว | เขียนไม่ได้ต้องไม่รายงานว่ารับสำเร็จ ตรวจอ่านหลัง restart | เก็บข้อมูลระหว่าง offline |
| 18 offline และส่งย้อนหลัง | หยุด broker ในเครื่อง เริ่มใหม่แล้ว retry แบบหน่วงเวลา | ข้อมูลค้างส่งต่อได้ ใช้ ID และเวลาอ่านเดิม | store-and-forward |
| 19 ข้อมูลซ้ำและ ACK สองระดับ | จำลองผู้รับบันทึกฐานข้อมูล แยก MQTT PUBACK กับ application ACK | ผู้รับบันทึกแล้ว ACK หาย ส่งซ้ำแล้วไม่เพิ่มรายการซ้ำ | กำหนดเงื่อนไขนำข้อมูลออกจากคิวอย่างชัดเจน |
| 20 ทดสอบความขัดข้องและพื้นที่ | restart ตามจุดสำคัญ ACK หาย ไฟล์ท้ายขาด และคิวเต็ม | ทดสอบด้วยเพดานขนาดคิวเล็ก ไม่ทำดิสก์จริงเต็ม | เห็นข้อจำกัดและนโยบายรับข้อมูลเมื่อพื้นที่ไม่พอ |

### ความรู้ที่เชื่อมต่อกัน

- รู้จัก module เพียงพอรันตั้งแต่บท 1 แต่เก็บรายละเอียดไว้ EP21–22 เพื่อไม่ให้เครื่องมือขวางการเขียนโปรแกรมแรก
- แนะนำ event ID และเวลาอ่านเดิมตั้งแต่บท 14 เพราะข้อมูลที่เก็บลงคิวต้องมีตัวตนก่อน retry แล้วเจาะการใช้กันซ้ำในบท 19
- บท 16 เรียนช่องทาง MQTT ให้เข้าใจก่อน บท 17 จึงเพิ่มความทนต่อการหลุด โดยกำหนดกติกาบันทึกก่อนส่งตั้งแต่ต้น
- ในบท MQTT จะอธิบายว่า PUBACK ของ QoS 1 เป็นการยืนยันในระดับ MQTT ไม่ใช่หลักฐานว่าฐานข้อมูลปลายทางบันทึกแล้ว ส่วนบท 19 จะลงมือทดสอบ application ACK หลังบันทึกฐานข้อมูล
- ค่อยเพิ่ม interface และการทดสอบงานพร้อมกันเมื่อมีปัญหาจริงให้แก้ ไม่เริ่มด้วย framework

ก่อนลงมือบท MQTT ต้องตรวจเอกสาร protocol/client/broker รุ่นที่เลือกอีกครั้ง และแจ้งขั้นตอนติดตั้ง broker ก่อนปรับระบบ ใช้เฉพาะ loopback ไม่เปิดพอร์ตสู่เครือข่ายภายนอก ยังไม่เลือกหรืออ้างว่าทดสอบเครื่องมือเหล่านั้นแล้ว

## ตำแหน่งเอกสารและงานฝึก

`docs/playlist-01-go-basic/` เป็นสารบัญเฟสแรก ส่วน Phase 2–5 ยังเก็บเป็นแผนใน `docs/FUTURE_ROADMAP.md` ไม่สร้างบทเรียนว่างจำนวนมาก ลิงก์ไปเนื้อหาจริงใน lessons งานฝึกใช้ `practics/` และถูก ignore ทั้งหมด ดู [วิธีใช้ practics](docs/PRACTICE.md)

## กติกาก่อนผ่านแต่ละบท

อธิบายแนวคิดได้ด้วยคำตนเอง รันตัวอย่าง ทำแบบฝึกหัด 2–3 ข้อ ตรวจ error อย่างน้อยหนึ่งกรณี และบันทึกทั้งผลสำเร็จกับส่วนที่ยังไม่ทดสอบ ทบทวนส่วนที่ยังไม่เข้าใจก่อนเรียนเรื่องถัดไป

อ้างอิงหลักที่จะใช้ตามหัวข้อ: [Go tutorials](https://go.dev/doc/tutorial/), [Language specification](https://go.dev/ref/spec), [Standard library](https://pkg.go.dev/std)
