# แผนหลักสูตร 5 Phase — Go Basic → API → Database → Gateway

สถานะ: Phase 1 มีบทเรียนครบ EP1–28 ส่วน Phase 2–5 ยังเป็นแผน เลือกเรียนตามลำดับและทบทวนได้ตามความพร้อมของตนเอง

## เส้นทางการเรียนรู้

| Phase | ขอบเขต | ผลลัพธ์ปลายเฟส | สถานะบทเรียน |
|---|---|---|---|
| 1 — Basic | พื้นฐานภาษาและโปรเจกต์รายงานข้อมูลจำลอง | อธิบายและเขียนโปรแกรมเล็กได้เอง | พร้อมเรียน EP1–28 |
| 2 — Practical Go | configuration, JSON, ไฟล์, HTTP client, งานพร้อมกัน | โปรแกรมที่จัดการข้อมูลและ error เป็นระบบ | แผน |
| 3 — Back-end & API | HTTP server, JSON, validation, CRUD ในหน่วยความจำ, test | API บน localhost | แผน |
| 4 — Database | SQL, database/sql, SQLite, migration, transaction, event ID | API ที่ข้อมูลยังอยู่หลัง restart | แผน |
| 5 — IoT / Edge Gateway | อุปกรณ์จำลอง, MQTT, queue, offline, ACK | ส่งข้อมูลย้อนหลังและทดสอบความขัดข้อง | แผน |

เริ่มที่ [Phase 1 — พื้นฐาน Go](docs/playlist-01-go-basic/README.md) รายละเอียด Phase 2–5 อยู่ใน [แผนการเรียนต่อ](docs/FUTURE_ROADMAP.md) จำนวน EP ของเฟสถัดไปจะกำหนดเมื่อจัดทำบทเรียน

เรียนการสร้าง API และใช้ฐานข้อมูลก่อน Gateway เพื่อทำความเข้าใจทีละส่วน และนำบริการที่บันทึกข้อมูลได้แล้วมาใช้รับข้อมูลจากอุปกรณ์จำลอง

## Phase 1: Basic — EP1–28

เตรียมเป็นตอนเล็กครบแล้ว ดู [สารบัญทุก EP](docs/playlist-01-go-basic/README.md) สำหรับลิงก์บทเรียน แบบฝึกหัด และเฉลย

| หัวข้อ | EP | สิ่งที่ได้ทำ | นำไปต่อยอด |
|---|---|---|---|
| Hello Go | 1–4 | รัน อ่านโค้ด พิมพ์ข้อความ และอ่าน error | ตรวจเครื่องมือและไล่ปัญหา |
| ตัวแปรและชนิดข้อมูล | 5–9 | ตัวแปร ชนิด ค่าศูนย์ รูปแบบผล คำนวณ | ข้อมูลการวัดและหน่วย |
| เงื่อนไขและลูป | 10–11 | ตรวจเกณฑ์และทำซ้ำ | ประมวลผลข้อมูลหลายรายการ |
| ฟังก์ชันและ error | 12–14 | รับค่า คืนค่า ตรวจ error | แยกหน้าที่และปฏิเสธข้อมูลเสีย |
| array, slice, map | 15–17 | กลุ่มข้อมูลและค้นหาด้วยชื่อ | รายการและสถานะอุปกรณ์ |
| struct, method, pointer | 18–20 | แบบข้อมูล พฤติกรรม และการแก้ต้นฉบับ | ข้อมูลการวัดที่มีความหมาย |
| package และ module | 21–22 | แยกไฟล์ อ่าน import และ go.mod | โปรแกรมที่โตขึ้น |
| unit test | 23–24 | test แรกและค่าขอบเขต | ตรวจเมื่อแก้โค้ด |
| โปรเจกต์สรุป | 25–28 | รายงานค่าจำลอง ตรวจข้อมูล ทำต่อหลังพบรายการเสีย และ test | Application Core ใน Phase 2 |

เตรียมหลาย EP แต่เรียนทีละ EP ได้ การแยก input/output ของฟังก์ชัน และการแยกงานโปรเจกต์ 4 ขั้น ช่วยไม่ให้มีหลายแนวคิดใหม่ในคราวเดียว ทุกขั้นใช้ข้อมูลจำลองในหน่วยความจำ ไม่ใช้เครือข่ายหรือฐานข้อมูล

## Phase 2: Practical Go — โปรแกรมที่ใช้งานได้จริง

| หัวข้อ | เป้าหมายและงานลงมือทำ | ตรวจว่าทำได้ | นำไปต่อยอด |
|---|---|---|---|
| configuration และ JSON | อ่านรายการอุปกรณ์จำลองจากไฟล์ | JSON เสีย ช่องข้อมูลหาย และค่าที่ใช้ไม่ได้ | เปลี่ยนรายการอุปกรณ์โดยไม่แก้โค้ด |
| ไฟล์และ log | เขียน/อ่านบันทึก ปิดไฟล์ ตรวจ error | ไฟล์หาย สิทธิ์เขียนไม่พอ ข้อมูลอ่านกลับตรงกัน | พื้นฐานคิวบนดิสก์และการตามปัญหา |
| HTTP และ timeout | เรียก server จำลองที่ localhost | server ช้า คืนสถานะผิด และปิดอยู่ | จำกัดเวลารอแหล่งข้อมูล |
| goroutine, channel, context | อธิบายแต่ละศัพท์ก่อนแบ่งงาน รับผล และยกเลิก | งานถูกยกเลิกได้ ไม่มีการรอไม่สิ้นสุด | อ่านหลายอุปกรณ์พร้อมกัน |
| graceful shutdown และงานพร้อมกัน | หยุดรับงาน รอส่วนที่ต้องจบ ปิดทรัพยากร | ทดสอบการหยุดและตรวจ data race เมื่อเครื่องรองรับ | ปิด gateway อย่างมีลำดับ |

ก่อนเรื่องการหยุดโปรแกรม จะเรียน **interface**: ข้อตกลงว่าค่าหนึ่งเรียก method อะไรได้ ใช้สลับงานจริงกับตัวจำลองในการทดสอบ โดยมีพื้นฐาน method จาก Phase 1

## Phase 3: Back-end และ REST API

เปิด server ตอบข้อความหนึ่งเส้นทาง → JSON หนึ่งรายการ → ใช้กฎจาก Phase 2 → เพิ่มข้อมูลในหน่วยความจำ → ตรวจ request และตอบ error → CRUD แยกทีละงาน → ทดสอบ handler และหยุด server เริ่มด้วย net/http ไม่บังคับ framework ยังไม่ใช้ฐานข้อมูลใน API แรก ดูขอบเขตและเกณฑ์ตรวจใน [Future Roadmap](docs/FUTURE_ROADMAP.md)

## Phase 4: Database และการเก็บข้อมูลถาวร

รู้จักตาราง/SQL → เขียนและอ่านหนึ่งรายการ → Go database/sql กับ driver → ต่อ API ให้ข้อมูลอยู่หลัง restart → migration → transaction → ป้องกัน event ซ้ำ → ค้นประวัติและสำรองข้อมูล เริ่มวางแผนด้วย SQLite, PostgreSQL เป็นทางเลือกภายหลัง ก่อนลงมือจะตรวจ driver และเครื่องมือกับ Go รุ่นจริงอีกครั้ง รายละเอียดใน [Future Roadmap](docs/FUTURE_ROADMAP.md)

## Phase 5: IoT / Edge Gateway จำลอง

| หัวข้อ | เป้าหมายและงานลงมือทำ | ตรวจว่าทำได้ | นำไปต่อยอด |
|---|---|---|---|
| อุปกรณ์จำลอง | สร้าง Measurement จากค่าที่ควบคุมได้ กำหนด event ID และเวลาอ่านเดิม | ค่าและเวลาตรวจซ้ำได้ ID แยกเหตุการณ์ได้ | ข้อมูลชุดเดียวกันตลอดการส่งและ retry |
| หลายอุปกรณ์ | timeout แยกอุปกรณ์ รวบรวมผลที่สำเร็จ | ตัวหนึ่งล้มแล้วตัวอื่นยังให้ผล | แยกผลกระทบของความเสียหาย |
| MQTT บนเครื่องตัวเอง | อธิบาย broker, topic, publish, subscribe และ QoS ก่อนใช้ | ส่งข้อมูลจำลองถึง subscriber บน loopback | ช่องทางส่งข้อมูลที่มีผู้รับ |
| บันทึกก่อนส่ง | append ลงคิวบนดิสก์ก่อนส่ง อธิบายจุดที่ถือว่ารับข้อมูลแล้ว | เขียนไม่ได้ต้องไม่รายงานว่ารับสำเร็จ ตรวจอ่านหลัง restart | เก็บข้อมูลระหว่าง offline |
| offline และส่งย้อนหลัง | หยุด broker ในเครื่อง เริ่มใหม่แล้ว retry แบบหน่วงเวลา | ข้อมูลค้างส่งต่อได้ ใช้ ID และเวลาอ่านเดิม | store-and-forward |
| ข้อมูลซ้ำและ ACK สองระดับ | จำลองผู้รับบันทึกฐานข้อมูล แยก MQTT PUBACK กับ application ACK | ผู้รับบันทึกแล้ว ACK หาย ส่งซ้ำแล้วไม่เพิ่มรายการซ้ำ | กำหนดเงื่อนไขนำข้อมูลออกจากคิวอย่างชัดเจน |
| ทดสอบความขัดข้องและพื้นที่ | restart ตามจุดสำคัญ ACK หาย ไฟล์ท้ายขาด และคิวเต็ม | ทดสอบด้วยเพดานขนาดคิวเล็ก ไม่ทำดิสก์จริงเต็ม | เห็นข้อจำกัดและนโยบายรับข้อมูลเมื่อพื้นที่ไม่พอ |

### ความรู้ที่เชื่อมต่อกัน

- เริ่มฝึกด้วย `main.go` ไฟล์เดียว แล้วค่อยเรียน package และ module ใน EP.21–22
- เริ่มกำหนด event ID และเวลาอ่านตั้งแต่สร้างอุปกรณ์จำลอง เพื่อใช้ข้อมูลเดิมเมื่อต้องส่งซ้ำ
- เรียนการส่งข้อมูลผ่าน MQTT ก่อน แล้วจึงเพิ่มคิวบนดิสก์เพื่อเก็บข้อมูลก่อนส่ง
- ในบท MQTT จะอธิบายว่า PUBACK ของ QoS 1 เป็นการยืนยันในระดับ MQTT ไม่ใช่หลักฐานว่าฐานข้อมูลปลายทางบันทึกแล้ว จากนั้นจะทดลองส่งคำยืนยันหลังบันทึกฐานข้อมูล หรือ application ACK
- ค่อยเพิ่ม interface และการทดสอบงานพร้อมกันเมื่อมีปัญหาจริงให้แก้ ไม่เริ่มด้วย framework

ก่อนเริ่มเรื่อง MQTT ต้องตรวจเอกสาร protocol/client/broker รุ่นที่เลือกอีกครั้ง และแจ้งขั้นตอนติดตั้ง broker ก่อนปรับระบบ ใช้เฉพาะ loopback ไม่เปิดพอร์ตสู่เครือข่ายภายนอก ยังไม่เลือกหรืออ้างว่าทดสอบเครื่องมือเหล่านั้นแล้ว

## ตำแหน่งเอกสารและงานฝึก

เปิดบทเรียนจาก [สารบัญ Phase 1](docs/playlist-01-go-basic/README.md) และทำโจทย์ใน `practics/main.go` ไฟล์เดิมตาม [วิธีฝึก](docs/PRACTICE.md) ส่วนเฟสถัดไปอ่านแผนได้ใน [Phase 2–5](docs/FUTURE_ROADMAP.md)

## ทบทวนก่อนเรียนต่อ

ลองอธิบายแนวคิดด้วยคำของตัวเอง รันตัวอย่าง และทำแบบฝึกหัดก่อนดูเฉลย หากผลไม่ตรงกับที่คาด ให้ลองหาสาเหตุและแก้ทีละจุด ทบทวนส่วนที่ยังไม่เข้าใจก่อนเรียนเรื่องถัดไป

อ้างอิงหลักที่จะใช้ตามหัวข้อ: [Go tutorials](https://go.dev/doc/tutorial/), [Language specification](https://go.dev/ref/spec), [Standard library](https://pkg.go.dev/std)
