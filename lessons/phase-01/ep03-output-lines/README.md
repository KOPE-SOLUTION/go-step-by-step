# EP.3 — พิมพ์หลายบรรทัดและอ่านลำดับคำสั่ง

Phase 1 — Go Basic • [สารบัญ](../../../docs/playlist-01-go-basic/README.md)

## เป้าหมายและสิ่งที่ควรรู้ก่อน

ทายลำดับข้อความและแยก Print กับ Println ได้ ก่อนเริ่มควรทำ EP.2 ได้และอธิบายตัวอย่างด้วยคำของตนเอง ไม่จำเป็นต้องจำโค้ดจากความจำ

บทนี้มีหนึ่งเรื่องหลัก อ่านเป็นช่วงได้: ทำความเข้าใจ → คาดเดา → รัน → เปลี่ยนทีละจุด ถ้าต้องกลับไปทบทวนใช้ [ตอนก่อนหน้า](../ep02-read-program/README.md)

## แนวคิดและคำใหม่

เหมือนรายการงานบนกระดาษ โปรแกรมสั้นนี้ทำบรรทัดบนก่อนบรรทัดล่าง การเห็นคำว่า READY เป็นเพียงข้อความ ไม่ได้หมายความว่าเชื่อมต่ออุปกรณ์แล้ว

**ลำดับคำสั่ง** คือก่อน–หลังที่โค้ดส่วนนี้ทำงาน; `Print` แสดงค่าโดยไม่เติมบรรทัดใหม่ ส่วน `Println` เติมบรรทัดใหม่หลังค่า

## หยุดคาดเดาก่อนเปิดผล

เรียกฟังก์ชันพิมพ์ 3 ครั้ง จะได้ 3 บรรทัดเสมอหรือไม่?

จดคำตอบสั้น ๆ ก่อนรัน ผิดได้ เก็บเหตุผลที่คิดไว้เทียบกับสิ่งที่เกิดขึ้น

## ตัวอย่างเล็กที่รันได้

### examples/main.go

```go
package main

import "fmt"

func main() {
	fmt.Print("Device: ")
	fmt.Println("sensor-01")
	fmt.Println("READY")
}
```

## รันและตรวจผล

เปิด PowerShell แล้วเลือกโฟลเดอร์บทนี้ (เครื่องอื่นให้เปลี่ยนส่วนหน้าของตำแหน่งตามที่วางหลักสูตร):

```powershell
Set-Location -LiteralPath 'C:\Users\kopes\Documents\Codex\2026-09-17\go-youtube-go-iot-edge-gateway\Go\lessons\phase-01\ep03-output-lines'
go run ./examples
```

ผลที่คาดหวัง:

```text
Device: sensor-01
READY
```

ถ้าผลต่าง ให้ตรวจตำแหน่งด้วย `Get-Location` ตรวจไฟล์ที่บันทึก และอ่าน error แรกก่อนแก้

## ทำไมโค้ดจึงทำงานแบบนี้

- Print แรกยังไม่ขึ้นบรรทัดใหม่ ข้อความจาก Println ถัดไปจึงต่อในบรรทัดเดียวกัน
- เว้นวรรคหลัง `Device:` อยู่ใน string ถ้าเอาออกข้อความจะติดกัน
- ตัวอย่างนี้ไม่มีงานพร้อมกัน ลำดับการเรียกทั้งสามจึงอ่านจากบนลงล่างได้

**แยกประเภท:** syntax และชนิดข้อมูลเป็นหลักของภาษา; การแยกหน้าที่ให้ส่วนเล็กอ่านง่ายเป็นแนวทางออกแบบ; ชื่อโฟลเดอร์และการใช้หนึ่ง module ต่อ EP เป็นทางเลือกของหลักสูตร เกณฑ์อุณหภูมิที่พบเป็นข้อกำหนดจำลอง

## ลองแก้ในพื้นที่ฝึก

เปิด [วิธีสร้างพื้นที่ฝึก](../../../docs/PRACTICE.md) แล้วสร้าง EP.3 ด้วย `./scripts/new-practice.ps1 -Episode 3` จากราก Go เพียงครั้งเดียว จากนั้นเปิด `practics/phase-01/ep03-output-lines` แก้ไฟล์ใน `examples` แล้วรัน `go run ./examples` จากโฟลเดอร์ EP ที่คัดลอกแล้ว

พื้นที่ฝึกไม่รวมเฉลย และคำสั่งสร้างจะหยุดถ้าโฟลเดอร์มีอยู่แล้ว จึงไม่ทับงานที่เคยทำ

## ข้อผิดพลาดที่พบบ่อย

- ใช้ Print แล้วข้อความติดกัน: ไม่มีการเติม newline ให้อัตโนมัติ
- ลืมช่องว่างภายใน quote: โปรแกรมทำตาม string ที่เขียนจริง

## แบบฝึกหัดและคำถามทบทวน

ทำ [แบบฝึกหัด 2 ข้อ](exercises/README.md) ก่อนดูเฉลย คาดผลของแต่ละข้อแล้วเปลี่ยนเพียงจุดที่โจทย์ระบุ

คำถามตรวจความเข้าใจ: จำนวนครั้งที่เรียกฟังก์ชันพิมพ์เท่ากับจำนวนบรรทัดเสมอไหม?

[เฉลยและเหตุผล](solutions/README.md) แยกไว้เพื่อเปิดหลังลองเอง ถ้ายังไม่ผ่าน ให้จดสิ่งที่คาดกับผลจริงไว้ก่อนขอความช่วยเหลือ

## สิ่งที่ได้และขอบเขต

อ่านลำดับผลลัพธ์ได้ เป็นพื้นฐานรายงาน Console

ใช้ข้อมูลจำลองภายในโปรแกรมเท่านั้น ยังไม่ได้อ่านอุปกรณ์ ส่ง MQTT เรียก API หรือเก็บฐานข้อมูล

ผลตรวจจริงและส่วนที่ยังไม่ได้ทดสอบ: [tests/RESULTS.md](tests/RESULTS.md)

อ้างอิงหลัก: [เอกสาร Go ทางการ](https://pkg.go.dev/fmt)

เมื่ออธิบายผลและทำแบบฝึกหัดได้ ค่อยเลือก [ตอนถัดไป / สารบัญ](../ep04-read-errors/README.md) ไม่มีข้อกำหนดให้เรียนหลายตอนในวันเดียว
