# EP.23 — เขียน unit test แรก

Phase 1 — Go Basic • [สารบัญ](../../../docs/playlist-01-go-basic/README.md)

## เป้าหมายและสิ่งที่ควรรู้ก่อน

ให้ Go ตรวจคำตอบของฟังก์ชันแทนการมองผลเอง ก่อนเริ่มควรทำ EP.22 ได้และอธิบายตัวอย่างด้วยคำของตนเอง ไม่จำเป็นต้องจำโค้ดจากความจำ

บทนี้มีหนึ่งเรื่องหลัก อ่านเป็นช่วงได้: ทำความเข้าใจ → คาดเดา → รัน → เปลี่ยนทีละจุด ถ้าต้องกลับไปทบทวนใช้ [ตอนก่อนหน้า](../ep22-modules/README.md)

## แนวคิดและคำใหม่

แทนการกดเครื่องคิดเลขแล้วดูด้วยตาทุกครั้ง เราเขียนข้อสอบพร้อมคำตอบที่คาดไว้ ถ้าโปรแกรมเปลี่ยนจนตอบผิด การทดสอบจะแจ้งให้เห็น

unit test ตรวจหน่วยงานเล็ก เช่น ฟังก์ชัน; ไฟล์ _test.go ใช้เก็บ test; testing เป็น package มาตรฐาน; Test... เป็นชื่อฟังก์ชันทดสอบ; t *testing.T เป็นตัวช่วยรายงานผลที่ Go ส่งให้; Fatalf รายงานและหยุด test นั้น; %v แสดงค่าทั่วไป

## หยุดคาดเดาก่อนเปิดผล

เปลี่ยน return celsius >= 30 เป็น > 30 แล้ว test ค่า 30 ควรผ่านหรือไม่?

จดคำตอบสั้น ๆ ก่อนรัน ผิดได้ เก็บเหตุผลที่คิดไว้เทียบกับสิ่งที่เกิดขึ้น

## ตัวอย่างเล็กที่รันได้

### examples/main.go

```go
package main

import "fmt"

func main() {
	fmt.Println(isWarning(30))
}
```

### examples/threshold.go

```go
package main

func isWarning(celsius float64) bool {
	return celsius >= 30
}
```

### examples/threshold_test.go

อ่านทีละฟังก์ชันทดสอบ พักและลองรันก่อนอ่านฟังก์ชันถัดไปได้

```go
package main

import "testing"

func TestIsWarning(t *testing.T) {
	got := isWarning(30)
	want := true
	if got != want {
		t.Fatalf("isWarning(30) = %v; want %v", got, want)
	}
}
```

## รันและตรวจผล

เปิด PowerShell แล้วเลือกโฟลเดอร์บทนี้ (เครื่องอื่นให้เปลี่ยนส่วนหน้าของตำแหน่งตามที่วางหลักสูตร):

```powershell
Set-Location -LiteralPath 'C:\Users\kopes\Documents\Codex\2026-09-17\go-youtube-go-iot-edge-gateway\Go\lessons\phase-01\ep23-first-test'
go run ./examples
```

ผลที่คาดหวัง:

```text
true
```

ถ้าผลต่าง ให้ตรวจตำแหน่งด้วย `Get-Location` ตรวจไฟล์ที่บันทึก และอ่าน error แรกก่อนแก้ รัน test เพิ่มจากตำแหน่งเดิม:

```powershell
go test -v ./examples
```

เมื่อผ่านจะมี PASS และบรรทัด ok; เวลาและรูปแบบบรรทัดเสริมอาจต่างตามเวอร์ชัน หากตั้งใจทำให้ผิด จะเห็น FAIL พร้อมกรณีที่ไม่ผ่าน

## ทำไมโค้ดจึงทำงานแบบนี้

- go test ./... ค้น package ภายใต้ module แล้วรันฟังก์ชันทดสอบ ไม่เรียก main เพื่อพิมพ์ตัวอย่าง
- got คือผลจริง want คือผลที่ข้อกำหนดต้องการ ตั้งชื่อตามแนวทางให้อ่านง่าย
- ไฟล์ .go ใน package เดียวกันใช้ฟังก์ชันร่วมกันได้ ไม่ต้อง import กันเอง; รัน go run ./examples เพื่อรวม main กับ threshold.go

**แยกประเภท:** syntax และชนิดข้อมูลเป็นหลักของภาษา; การแยกหน้าที่ให้ส่วนเล็กอ่านง่ายเป็นแนวทางออกแบบ; ชื่อโฟลเดอร์และการใช้หนึ่ง module ต่อ EP เป็นทางเลือกของหลักสูตร เกณฑ์อุณหภูมิที่พบเป็นข้อกำหนดจำลอง

## ลองแก้ในพื้นที่ฝึก

เปิด [วิธีสร้างพื้นที่ฝึก](../../../docs/PRACTICE.md) แล้วสร้าง EP.23 ด้วย `./scripts/new-practice.ps1 -Episode 23` จากราก Go เพียงครั้งเดียว จากนั้นเปิด `practics/phase-01/ep23-first-test` แก้ไฟล์ใน `examples` แล้วรัน `go run ./examples` จากโฟลเดอร์ EP ที่คัดลอกแล้ว

พื้นที่ฝึกไม่รวมเฉลย และคำสั่งสร้างจะหยุดถ้าโฟลเดอร์มีอยู่แล้ว จึงไม่ทับงานที่เคยทำ

## ข้อผิดพลาดที่พบบ่อย

- ตั้งชื่อไฟล์ threshold-test.go: ต้องลงท้าย _test.go
- ใช้ go run ./examples/main.go: จะไม่รวม threshold.go ให้รันทั้ง package ด้วย ./examples

## แบบฝึกหัดและคำถามทบทวน

ทำ [แบบฝึกหัด 2 ข้อ](exercises/README.md) ก่อนดูเฉลย คาดผลของแต่ละข้อแล้วเปลี่ยนเพียงจุดที่โจทย์ระบุ

คำถามตรวจความเข้าใจ: test ผ่านหนึ่งกรณีพิสูจน์ว่าฟังก์ชันถูกทุกค่าหรือไม่?

[เฉลยและเหตุผล](solutions/README.md) แยกไว้เพื่อเปิดหลังลองเอง ถ้ายังไม่ผ่าน ให้จดสิ่งที่คาดกับผลจริงไว้ก่อนขอความช่วยเหลือ

## สิ่งที่ได้และขอบเขต

สร้างหลักฐานที่ทำซ้ำได้ว่าฟังก์ชันตอบตรงตามกรณีที่กำหนด

ใช้ข้อมูลจำลองภายในโปรแกรมเท่านั้น ยังไม่ได้อ่านอุปกรณ์ ส่ง MQTT เรียก API หรือเก็บฐานข้อมูล

ผลตรวจจริงและส่วนที่ยังไม่ได้ทดสอบ: [tests/RESULTS.md](tests/RESULTS.md)

อ้างอิงหลัก: [เอกสาร Go ทางการ](https://go.dev/doc/tutorial/add-a-test)

เมื่ออธิบายผลและทำแบบฝึกหัดได้ ค่อยเลือก [ตอนถัดไป / สารบัญ](../ep24-boundary-tests/README.md) ไม่มีข้อกำหนดให้เรียนหลายตอนในวันเดียว
