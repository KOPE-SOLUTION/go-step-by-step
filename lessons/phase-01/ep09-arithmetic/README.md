# EP.9 — คำนวณและแปลงชนิดตัวเลข

Phase 1 — Go Basic • [สารบัญ](../../../docs/playlist-01-go-basic/README.md)

## เป้าหมายและสิ่งที่ควรรู้ก่อน

คำนวณค่าเฉลี่ยโดยรู้ว่าการหารจำนวนเต็มต่างจากทศนิยม ก่อนเริ่มควรทำ EP.8 ได้และอธิบายตัวอย่างด้วยคำของตนเอง ไม่จำเป็นต้องจำโค้ดจากความจำ

บทนี้มีหนึ่งเรื่องหลัก อ่านเป็นช่วงได้: ทำความเข้าใจ → คาดเดา → รัน → เปลี่ยนทีละจุด ถ้าต้องกลับไปทบทวนใช้ [ตอนก่อนหน้า](../ep08-format-output/README.md)

## แนวคิดและคำใหม่

ถ้าแบ่งของ 5 ชิ้นให้ 2 กลุ่มโดยนับเฉพาะชิ้นเต็ม จะได้กลุ่มละ 2 แต่การวัดอุณหภูมิต้องการส่วนเศษ จึงต้องเลือกชนิดก่อนหาร ไม่ใช่ค่อยแปลงหลังเศษหายไปแล้ว

`+ - * /` คือบวก ลบ คูณ หาร; **conversion** คือแปลงชนิดอย่างชัดเจน เช่น float64(total); **const** คือชื่อค่าคงที่ซึ่งกำหนดใหม่ไม่ได้

## หยุดคาดเดาก่อนเปิดผล

float64(total / count) จะยังได้ 2.5 หรือไม่?

จดคำตอบสั้น ๆ ก่อนรัน ผิดได้ เก็บเหตุผลที่คิดไว้เทียบกับสิ่งที่เกิดขึ้น

## ตัวอย่างเล็กที่รันได้

### examples/main.go

```go
package main

import "fmt"

func main() {
	const count = 2
	total := 5
	fmt.Println(total / count)
	average := float64(total) / count
	fmt.Printf("%.1f\n", average)
}
```

## รันและตรวจผล

เปิด PowerShell แล้วเลือกโฟลเดอร์บทนี้ (เครื่องอื่นให้เปลี่ยนส่วนหน้าของตำแหน่งตามที่วางหลักสูตร):

```powershell
Set-Location -LiteralPath 'C:\Users\kopes\Documents\Codex\2026-09-17\go-youtube-go-iot-edge-gateway\Go\lessons\phase-01\ep09-arithmetic'
go run ./examples
```

ผลที่คาดหวัง:

```text
2
2.5
```

ถ้าผลต่าง ให้ตรวจตำแหน่งด้วย `Get-Location` ตรวจไฟล์ที่บันทึก และอ่าน error แรกก่อนแก้

## ทำไมโค้ดจึงทำงานแบบนี้

- total เป็น int การหารในบรรทัดแรกจึงเป็นการหารจำนวนเต็ม ตัดส่วนเศษเข้าหาศูนย์
- float64(total) แปลงก่อนหารจึงรักษาผลเป็นทศนิยม
- count เป็นค่าคงที่ไม่มีชนิดระบุในตัวอย่างนี้ จึงใช้กับการหารทั้งสองแบบได้

**แยกประเภท:** syntax และชนิดข้อมูลเป็นหลักของภาษา; การแยกหน้าที่ให้ส่วนเล็กอ่านง่ายเป็นแนวทางออกแบบ; ชื่อโฟลเดอร์และการใช้หนึ่ง module ต่อ EP เป็นทางเลือกของหลักสูตร เกณฑ์อุณหภูมิที่พบเป็นข้อกำหนดจำลอง

## ลองแก้ในพื้นที่ฝึก

เปิด [วิธีสร้างพื้นที่ฝึก](../../../docs/PRACTICE.md) แล้วสร้าง EP.9 ด้วย `./scripts/new-practice.ps1 -Episode 9` จากราก Go เพียงครั้งเดียว จากนั้นเปิด `practics/phase-01/ep09-arithmetic` แก้ไฟล์ใน `examples` แล้วรัน `go run ./examples` จากโฟลเดอร์ EP ที่คัดลอกแล้ว

พื้นที่ฝึกไม่รวมเฉลย และคำสั่งสร้างจะหยุดถ้าโฟลเดอร์มีอยู่แล้ว จึงไม่ทับงานที่เคยทำ

## ข้อผิดพลาดที่พบบ่อย

- แปลงหลังหารจำนวนเต็ม: เศษหายไปแล้ว
- หารด้วยศูนย์: เป็นกรณีที่ต้องกันไว้เมื่อจำนวนมาจากข้อมูล ไม่ทดลองกับระบบจริง

## แบบฝึกหัดและคำถามทบทวน

ทำ [แบบฝึกหัด 2 ข้อ](exercises/README.md) ก่อนดูเฉลย คาดผลของแต่ละข้อแล้วเปลี่ยนเพียงจุดที่โจทย์ระบุ

คำถามตรวจความเข้าใจ: ทำไมต้องแปลงก่อนหารเมื่ออยากรักษาส่วนเศษ?

[เฉลยและเหตุผล](solutions/README.md) แยกไว้เพื่อเปิดหลังลองเอง ถ้ายังไม่ผ่าน ให้จดสิ่งที่คาดกับผลจริงไว้ก่อนขอความช่วยเหลือ

## สิ่งที่ได้และขอบเขต

คำนวณค่าการวัดโดยไม่เสียส่วนเศษโดยไม่ตั้งใจ

ใช้ข้อมูลจำลองภายในโปรแกรมเท่านั้น ยังไม่ได้อ่านอุปกรณ์ ส่ง MQTT เรียก API หรือเก็บฐานข้อมูล

ผลตรวจจริงและส่วนที่ยังไม่ได้ทดสอบ: [tests/RESULTS.md](tests/RESULTS.md)

อ้างอิงหลัก: [เอกสาร Go ทางการ](https://go.dev/tour/basics/13)

เมื่ออธิบายผลและทำแบบฝึกหัดได้ ค่อยเลือก [ตอนถัดไป / สารบัญ](../ep10-conditions/README.md) ไม่มีข้อกำหนดให้เรียนหลายตอนในวันเดียว
