# EP.21 — แยกงานเป็น package

Phase 1 — Go Basic • [สารบัญ](../../../docs/playlist-01-go-basic/README.md)

## เป้าหมายและสิ่งที่ควรรู้ก่อน

เรียกฟังก์ชันในโฟลเดอร์ sensor จาก main ก่อนเริ่มควรทำ EP.20 ได้และอธิบายตัวอย่างด้วยคำของตนเอง ไม่จำเป็นต้องจำโค้ดจากความจำ

บทนี้มีหนึ่งเรื่องหลัก อ่านเป็นช่วงได้: ทำความเข้าใจ → คาดเดา → รัน → เปลี่ยนทีละจุด ถ้าต้องกลับไปทบทวนใช้ [ตอนก่อนหน้า](../ep20-pointers/README.md)

## แนวคิดและคำใหม่

เมื่อโต๊ะเริ่มมีหลายชิ้นงาน เราแยกกลุ่มให้มีหน้าที่ชัดเจน package รวมไฟล์ Go ในโฟลเดอร์หนึ่งเพื่อให้โค้ดอื่น import มาใช้

package คือกลุ่มไฟล์ที่ทำงานร่วมกันในโฟลเดอร์; import path คือชื่อที่ระบุแหล่ง package; ชื่อที่ขึ้นต้นตัวใหญ่ เช่น IsWarning ถูก export ให้ package อื่นใช้; module คือชุด package ที่มี go.mod เป็นขอบเขต จะอ่านรายละเอียดใน EP22

## หยุดคาดเดาก่อนเปิดผล

ทำไมใช้ sensor.IsWarning แทนเรียก IsWarning ตรง ๆ?

จดคำตอบสั้น ๆ ก่อนรัน ผิดได้ เก็บเหตุผลที่คิดไว้เทียบกับสิ่งที่เกิดขึ้น

## ตัวอย่างเล็กที่รันได้

### examples/main.go

```go
package main

import (
	"fmt"

	"example.com/go-course/basic/ep21/sensor"
)

func main() {
	fmt.Println(sensor.IsWarning(30))
}
```

### sensor/reading.go

```go
package sensor

func IsWarning(celsius float64) bool {
	return celsius >= 30
}
```

## รันและตรวจผล

เปิด PowerShell แล้วเลือกโฟลเดอร์บทนี้ (เครื่องอื่นให้เปลี่ยนส่วนหน้าของตำแหน่งตามที่วางหลักสูตร):

```powershell
Set-Location -LiteralPath 'C:\Users\kopes\Documents\Codex\2026-09-17\go-youtube-go-iot-edge-gateway\Go\lessons\phase-01\ep21-packages'
go run ./examples
```

ผลที่คาดหวัง:

```text
true
```

ถ้าผลต่าง ให้ตรวจตำแหน่งด้วย `Get-Location` ตรวจไฟล์ที่บันทึก และอ่าน error แรกก่อนแก้

## ทำไมโค้ดจึงทำงานแบบนี้

- sensor/reading.go ประกาศ package sensor ส่วน examples/main.go เป็น package main คนละโฟลเดอร์
- import ใช้ชื่อ module ตาม go.mod ต่อด้วย /sensor ไม่ใช่ path ของไดรฟ์ C:
- โค้ด sensor อยู่บนเครื่องใน module เดียวกัน จึงไม่ต้องดาวน์โหลด; แยกเมื่อมีหน้าที่ชัดเจน ไม่จำเป็นต้องแยกทุกฟังก์ชัน

**แยกประเภท:** syntax และชนิดข้อมูลเป็นหลักของภาษา; การแยกหน้าที่ให้ส่วนเล็กอ่านง่ายเป็นแนวทางออกแบบ; ชื่อโฟลเดอร์และการใช้หนึ่ง module ต่อ EP เป็นทางเลือกของหลักสูตร เกณฑ์อุณหภูมิที่พบเป็นข้อกำหนดจำลอง

## ลองแก้ในพื้นที่ฝึก

เปิด [วิธีสร้างพื้นที่ฝึก](../../../docs/PRACTICE.md) แล้วสร้าง EP.21 ด้วย `./scripts/new-practice.ps1 -Episode 21` จากราก Go เพียงครั้งเดียว จากนั้นเปิด `practics/phase-01/ep21-packages` แก้ไฟล์ใน `examples` แล้วรัน `go run ./examples` จากโฟลเดอร์ EP ที่คัดลอกแล้ว โฟลเดอร์ sensor ต้องอยู่ด้วย สคริปต์คัดลอกให้แล้ว

พื้นที่ฝึกไม่รวมเฉลย และคำสั่งสร้างจะหยุดถ้าโฟลเดอร์มีอยู่แล้ว จึงไม่ทับงานที่เคยทำ

## ข้อผิดพลาดที่พบบ่อย

- เปลี่ยน IsWarning เป็น isWarning แล้วเรียกจาก main: ชื่อเล็กไม่ถูก export
- เอา package sensor และ package main ไว้โฟลเดอร์เดียวกัน: แยกตามต้นไม้ไฟล์ในบท

## แบบฝึกหัดและคำถามทบทวน

ทำ [แบบฝึกหัด 2 ข้อ](exercises/README.md) ก่อนดูเฉลย คาดผลของแต่ละข้อแล้วเปลี่ยนเพียงจุดที่โจทย์ระบุ

คำถามตรวจความเข้าใจ: ถ้าเปลี่ยนชื่อฟังก์ชันเป็น isWarning ทำไมอีก package เรียกไม่ได้?

[เฉลยและเหตุผล](solutions/README.md) แยกไว้เพื่อเปิดหลังลองเอง ถ้ายังไม่ผ่าน ให้จดสิ่งที่คาดกับผลจริงไว้ก่อนขอความช่วยเหลือ

## สิ่งที่ได้และขอบเขต

แยกตรรกะที่ใช้ซ้ำจากโปรแกรมหลัก โดยยังมีโครงสร้างเพียงสองโฟลเดอร์

ใช้ข้อมูลจำลองภายในโปรแกรมเท่านั้น ยังไม่ได้อ่านอุปกรณ์ ส่ง MQTT เรียก API หรือเก็บฐานข้อมูล

ผลตรวจจริงและส่วนที่ยังไม่ได้ทดสอบ: [tests/RESULTS.md](tests/RESULTS.md)

อ้างอิงหลัก: [เอกสาร Go ทางการ](https://go.dev/tour/basics/3)

เมื่ออธิบายผลและทำแบบฝึกหัดได้ ค่อยเลือก [ตอนถัดไป / สารบัญ](../ep22-modules/README.md) ไม่มีข้อกำหนดให้เรียนหลายตอนในวันเดียว
