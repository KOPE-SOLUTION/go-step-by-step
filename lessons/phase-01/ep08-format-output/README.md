# EP.8 — จัดข้อความด้วย Printf

Phase 1 — Go Basic • [สารบัญ](../../../docs/playlist-01-go-basic/README.md)

## เป้าหมายและสิ่งที่ควรรู้ก่อน

ประกอบรายงานจากค่าหลายชนิดด้วย format ที่ตรงกัน ก่อนเริ่มควรทำ EP.7 ได้และอธิบายตัวอย่างด้วยคำของตนเอง ไม่จำเป็นต้องจำโค้ดจากความจำ

บทนี้มีหนึ่งเรื่องหลัก อ่านเป็นช่วงได้: ทำความเข้าใจ → คาดเดา → รัน → เปลี่ยนทีละจุด ถ้าต้องกลับไปทบทวนใช้ [ตอนก่อนหน้า](../ep07-zero-values/README.md)

## แนวคิดและคำใหม่

เหมือนประโยคที่เว้นช่องให้เติมชื่อกับอุณหภูมิ Printf ใช้ข้อความรูปแบบกำหนดว่าค่าแต่ละตัวจะไปตรงไหน เหมาะกับรายงานที่ต้องอ่านซ้ำหลายรายการ

**format string** คือข้อความที่มีช่องแทนค่า; `%s` string, `%d` int, `%.1f` ทศนิยมหนึ่งตำแหน่ง, `%t` bool, `%q` ข้อความแบบมี quote; `\n` ขึ้นบรรทัดใหม่

## หยุดคาดเดาก่อนเปิดผล

27.56 จะแสดงเป็น 27.5 หรือ 27.6 เมื่อใช้ %.1f?

จดคำตอบสั้น ๆ ก่อนรัน ผิดได้ เก็บเหตุผลที่คิดไว้เทียบกับสิ่งที่เกิดขึ้น

## ตัวอย่างเล็กที่รันได้

### examples/main.go

```go
package main

import "fmt"

func main() {
	name := "sensor-01"
	temperature := 27.56
	online := true
	fmt.Printf("%s: %.1f C, online=%t\n", name, temperature, online)
}
```

## รันและตรวจผล

เปิด PowerShell แล้วเลือกโฟลเดอร์บทนี้ (เครื่องอื่นให้เปลี่ยนส่วนหน้าของตำแหน่งตามที่วางหลักสูตร):

```powershell
Set-Location -LiteralPath 'C:\Users\kopes\Documents\Codex\2026-09-17\go-youtube-go-iot-edge-gateway\Go\lessons\phase-01\ep08-format-output'
go run ./examples
```

ผลที่คาดหวัง:

```text
sensor-01: 27.6 C, online=true
```

ถ้าผลต่าง ให้ตรวจตำแหน่งด้วย `Get-Location` ตรวจไฟล์ที่บันทึก และอ่าน error แรกก่อนแก้

## ทำไมโค้ดจึงทำงานแบบนี้

- ค่าหลัง format string จับคู่กับช่องจากซ้ายไปขวา
- %.1f เปลี่ยนรูปที่แสดง ไม่ได้เปลี่ยนค่าตัวแปร temperature
- Printf ไม่เติม newline เอง จึงใส่ \n ท้ายข้อความ

**แยกประเภท:** syntax และชนิดข้อมูลเป็นหลักของภาษา; การแยกหน้าที่ให้ส่วนเล็กอ่านง่ายเป็นแนวทางออกแบบ; ชื่อโฟลเดอร์และการใช้หนึ่ง module ต่อ EP เป็นทางเลือกของหลักสูตร เกณฑ์อุณหภูมิที่พบเป็นข้อกำหนดจำลอง

## ลองแก้ในพื้นที่ฝึก

เปิด [วิธีสร้างพื้นที่ฝึก](../../../docs/PRACTICE.md) แล้วสร้าง EP.8 ด้วย `./scripts/new-practice.ps1 -Episode 8` จากราก Go เพียงครั้งเดียว จากนั้นเปิด `practics/phase-01/ep08-format-output` แก้ไฟล์ใน `examples` แล้วรัน `go run ./examples` จากโฟลเดอร์ EP ที่คัดลอกแล้ว

พื้นที่ฝึกไม่รวมเฉลย และคำสั่งสร้างจะหยุดถ้าโฟลเดอร์มีอยู่แล้ว จึงไม่ทับงานที่เคยทำ

## ข้อผิดพลาดที่พบบ่อย

- ลืม \n: prompt หรือข้อความถัดไปมาต่อบรรทัดเดียวกัน
- ใช้ %d กับ float64: ชนิดไม่ตรง มักเห็นข้อความ %! ในผลและ go vet ตรวจพบได้

## แบบฝึกหัดและคำถามทบทวน

ทำ [แบบฝึกหัด 2 ข้อ](exercises/README.md) ก่อนดูเฉลย คาดผลของแต่ละข้อแล้วเปลี่ยนเพียงจุดที่โจทย์ระบุ

คำถามตรวจความเข้าใจ: Printf %.1f ทำให้ค่าตัวแปรเหลือทศนิยมหนึ่งตำแหน่งด้วยหรือไม่?

[เฉลยและเหตุผล](solutions/README.md) แยกไว้เพื่อเปิดหลังลองเอง ถ้ายังไม่ผ่าน ให้จดสิ่งที่คาดกับผลจริงไว้ก่อนขอความช่วยเหลือ

## สิ่งที่ได้และขอบเขต

รายงานค่าการวัดให้อ่านได้ง่ายและสม่ำเสมอ

ใช้ข้อมูลจำลองภายในโปรแกรมเท่านั้น ยังไม่ได้อ่านอุปกรณ์ ส่ง MQTT เรียก API หรือเก็บฐานข้อมูล

ผลตรวจจริงและส่วนที่ยังไม่ได้ทดสอบ: [tests/RESULTS.md](tests/RESULTS.md)

อ้างอิงหลัก: [เอกสาร Go ทางการ](https://pkg.go.dev/fmt)

เมื่ออธิบายผลและทำแบบฝึกหัดได้ ค่อยเลือก [ตอนถัดไป / สารบัญ](../ep09-arithmetic/README.md) ไม่มีข้อกำหนดให้เรียนหลายตอนในวันเดียว
