# EP.11 — ทำงานซ้ำทีละรอบด้วย for

Phase 1 — Go Basic • [สารบัญ](../../../docs/playlist-01-go-basic/README.md)

## เป้าหมายและสิ่งที่ควรรู้ก่อน

ไล่รอบการทำงานและบอกจำนวนครั้งที่พิมพ์ได้ ก่อนเริ่มควรทำ EP.10 ได้และอธิบายตัวอย่างด้วยคำของตนเอง ไม่จำเป็นต้องจำโค้ดจากความจำ

บทนี้มีหนึ่งเรื่องหลัก อ่านเป็นช่วงได้: ทำความเข้าใจ → คาดเดา → รัน → เปลี่ยนทีละจุด ถ้าต้องกลับไปทบทวนใช้ [ตอนก่อนหน้า](../ep10-conditions/README.md)

## แนวคิดและคำใหม่

เหมือนตรวจอุปกรณ์หมายเลข 1 ถึง 3 ทีละตัว แทนที่จะเขียนคำสั่งพิมพ์ซ้ำ เราให้ตัวนับเดินไปทีละค่า

loop คืองานซ้ำ; for เริ่มด้วยค่าเริ่มต้น ตรวจเงื่อนไขก่อนแต่ละรอบ แล้วเปลี่ยนค่าหลังรอบ; i++ เพิ่ม i ขึ้น 1; <= คือไม่เกิน

## หยุดคาดเดาก่อนเปิดผล

หลังพิมพ์ sample 3 แล้ว i เพิ่มเป็น 4 จะพิมพ์อีกไหม?

จดคำตอบสั้น ๆ ก่อนรัน ผิดได้ เก็บเหตุผลที่คิดไว้เทียบกับสิ่งที่เกิดขึ้น

## ตัวอย่างเล็กที่รันได้

### examples/main.go

```go
package main

import "fmt"

func main() {
	for i := 1; i <= 3; i++ {
		fmt.Println("sample", i)
	}
}
```

## รันและตรวจผล

เปิด PowerShell แล้วเลือกโฟลเดอร์บทนี้ (เครื่องอื่นให้เปลี่ยนส่วนหน้าของตำแหน่งตามที่วางหลักสูตร):

```powershell
Set-Location -LiteralPath 'C:\Users\kopes\Documents\Codex\2026-09-17\go-youtube-go-iot-edge-gateway\Go\lessons\phase-01\ep11-loops'
go run ./examples
```

ผลที่คาดหวัง:

```text
sample 1
sample 2
sample 3
```

ถ้าผลต่าง ให้ตรวจตำแหน่งด้วย `Get-Location` ตรวจไฟล์ที่บันทึก และอ่าน error แรกก่อนแก้

## ทำไมโค้ดจึงทำงานแบบนี้

- i := 1 ทำครั้งเดียวก่อนเข้าลูป
- i <= 3 ถูกตรวจก่อนทุกรอบ ถ้า false จะออกทันที
- i++ ทำหลังจบเนื้อหาแต่ละรอบ; ตัวแปร i ในตัวอย่างใช้ได้ภายใน for ชุดนี้

**แยกประเภท:** syntax และชนิดข้อมูลเป็นหลักของภาษา; การแยกหน้าที่ให้ส่วนเล็กอ่านง่ายเป็นแนวทางออกแบบ; ชื่อโฟลเดอร์และการใช้หนึ่ง module ต่อ EP เป็นทางเลือกของหลักสูตร เกณฑ์อุณหภูมิที่พบเป็นข้อกำหนดจำลอง

## ลองแก้ในพื้นที่ฝึก

เปิด [วิธีสร้างพื้นที่ฝึก](../../../docs/PRACTICE.md) แล้วสร้าง EP.11 ด้วย `./scripts/new-practice.ps1 -Episode 11` จากราก Go เพียงครั้งเดียว จากนั้นเปิด `practics/phase-01/ep11-loops` แก้ไฟล์ใน `examples` แล้วรัน `go run ./examples` จากโฟลเดอร์ EP ที่คัดลอกแล้ว

พื้นที่ฝึกไม่รวมเฉลย และคำสั่งสร้างจะหยุดถ้าโฟลเดอร์มีอยู่แล้ว จึงไม่ทับงานที่เคยทำ

## ข้อผิดพลาดที่พบบ่อย

- ใช้ i < 3 แล้วหวังได้สามรอบจาก 1: จะได้เพียง 1 และ 2
- ลืมเพิ่มค่าในลูปแบบอื่นจนไม่จบ: หยุดด้วย Ctrl+C แล้วตรวจเงื่อนไข

## แบบฝึกหัดและคำถามทบทวน

ทำ [แบบฝึกหัด 2 ข้อ](exercises/README.md) ก่อนดูเฉลย คาดผลของแต่ละข้อแล้วเปลี่ยนเพียงจุดที่โจทย์ระบุ

คำถามตรวจความเข้าใจ: ค่าเริ่มต้น เงื่อนไข และการเพิ่มค่า ทำงานในลำดับใด?

[เฉลยและเหตุผล](solutions/README.md) แยกไว้เพื่อเปิดหลังลองเอง ถ้ายังไม่ผ่าน ให้จดสิ่งที่คาดกับผลจริงไว้ก่อนขอความช่วยเหลือ

## สิ่งที่ได้และขอบเขต

อ่านลำดับการทำซ้ำได้ เป็นฐานของการประมวลผลหลายรายการ

ใช้ข้อมูลจำลองภายในโปรแกรมเท่านั้น ยังไม่ได้อ่านอุปกรณ์ ส่ง MQTT เรียก API หรือเก็บฐานข้อมูล

ผลตรวจจริงและส่วนที่ยังไม่ได้ทดสอบ: [tests/RESULTS.md](tests/RESULTS.md)

อ้างอิงหลัก: [เอกสาร Go ทางการ](https://go.dev/tour/flowcontrol/1)

เมื่ออธิบายผลและทำแบบฝึกหัดได้ ค่อยเลือก [ตอนถัดไป / สารบัญ](../ep12-function-parameters/README.md) ไม่มีข้อกำหนดให้เรียนหลายตอนในวันเดียว
