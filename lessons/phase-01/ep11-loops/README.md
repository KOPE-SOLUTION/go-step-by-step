# EP.11 — ทำงานซ้ำทีละรอบด้วย for

**เป้าหมาย:** ไล่รอบการทำงานและบอกจำนวนครั้งที่พิมพ์ได้

## 1. อ่านโค้ด

`for` ทำงานซ้ำโดยตั้งค่าเริ่มต้น ตรวจเงื่อนไข แล้วเปลี่ยนค่าหลังจบแต่ละรอบ `i++` เพิ่มค่า 1 และ `<=` หมายถึงไม่เกิน

ไฟล์ [examples/main.go](examples/main.go)

```go
package main

import "fmt"

func main() {
	for i := 1; i <= 3; i++ {
		fmt.Println("sample", i)
	}
}
```

## 2. ลองรัน

**ก่อนรัน:** หลังพิมพ์ sample 3 แล้ว i เพิ่มเป็น 4 จะพิมพ์อีกไหม?

จากโฟลเดอร์หลักสูตรที่มี `lessons` เปิด PowerShell แล้วใช้:

```powershell
Set-Location -LiteralPath './lessons/phase-01/ep11-loops'
go run ./examples
```

<details>
<summary>ดูผลที่คาดหวังหลังลองรัน</summary>

```text
sample 1
sample 2
sample 3
```

เงื่อนไขถูกตรวจก่อนทุกรอบ รวมถึงรอบแรก

</details>

## 3. ฝึกเอง

ทำ [แบบฝึกหัด 2 ข้อ](exercises/README.md) ใน [practics](../../../docs/PRACTICE.md) แล้วลองตอบ: ค่าเริ่มต้น เงื่อนไข และการเพิ่มค่า ทำงานในลำดับใด?

<details>
<summary>อ่านเพิ่มเติมเมื่อสงสัย</summary>

เหมือนตรวจอุปกรณ์หมายเลข 1 ถึง 3 ทีละตัว แทนที่จะเขียนคำสั่งพิมพ์ซ้ำ เราให้ตัวนับเดินไปทีละค่า

loop คืองานซ้ำ; for เริ่มด้วยค่าเริ่มต้น ตรวจเงื่อนไขก่อนแต่ละรอบ แล้วเปลี่ยนค่าหลังรอบ; i++ เพิ่ม i ขึ้น 1; <= คือไม่เกิน

- i := 1 ทำครั้งเดียวก่อนเข้าลูป
- i <= 3 ถูกตรวจก่อนทุกรอบ ถ้า false จะออกทันที
- i++ ทำหลังจบเนื้อหาแต่ละรอบ; ตัวแปร i ในตัวอย่างใช้ได้ภายใน for ชุดนี้

**ข้อผิดพลาดที่พบบ่อย**

- ใช้ i < 3 แล้วหวังได้สามรอบจาก 1: จะได้เพียง 1 และ 2
- ลืมเพิ่มค่าในลูปแบบอื่นจนไม่จบ: หยุดด้วย Ctrl+C แล้วตรวจเงื่อนไข

[เฉลย](solutions/README.md) · [ผลตรวจและข้อจำกัด](tests/RESULTS.md) · [เอกสาร Go](https://go.dev/tour/flowcontrol/1)

</details>

[ตอนก่อนหน้า](../ep10-conditions/README.md) · [สารบัญ](../../../docs/playlist-01-go-basic/README.md) · [ตอนถัดไป](../ep12-function-parameters/README.md)
