# EP.10 — ตัดสินใจด้วย if และ else

Phase 1 — Go Basic • [สารบัญ](../../../docs/playlist-01-go-basic/README.md)

## เป้าหมายและสิ่งที่ควรรู้ก่อน

เลือกแสดงสถานะจากอุณหภูมิหนึ่งค่า ก่อนเริ่มควรทำ EP.9 ได้และอธิบายตัวอย่างด้วยคำของตนเอง ไม่จำเป็นต้องจำโค้ดจากความจำ

บทนี้มีหนึ่งเรื่องหลัก อ่านเป็นช่วงได้: ทำความเข้าใจ → คาดเดา → รัน → เปลี่ยนทีละจุด ถ้าต้องกลับไปทบทวนใช้ [ตอนก่อนหน้า](../ep09-arithmetic/README.md)

## แนวคิดและคำใหม่

เหมือนตรวจว่าต้องหยิบร่มหรือไม่ เราตั้งคำถามที่ตอบได้แค่จริงหรือเท็จ แล้วทำงานตามคำตอบ ตัวอย่างนี้กำหนดเกณฑ์เตือนสมมติที่ 30 องศา

เงื่อนไขคือข้อความที่ให้ค่า bool; >= หมายถึงมากกว่าหรือเท่ากับ; if ทำงานเมื่อเงื่อนไขเป็น true; else ทำงานในกรณีที่เหลือ

## หยุดคาดเดาก่อนเปิดผล

ค่า 30 พอดีจะเข้า WARNING หรือ OK เพราะอะไร?

จดคำตอบสั้น ๆ ก่อนรัน ผิดได้ เก็บเหตุผลที่คิดไว้เทียบกับสิ่งที่เกิดขึ้น

## ตัวอย่างเล็กที่รันได้

### examples/main.go

```go
package main

import "fmt"

func main() {
	temperature := 30.0
	if temperature >= 30 {
		fmt.Println("WARNING")
	} else {
		fmt.Println("OK")
	}
}
```

## รันและตรวจผล

เปิด PowerShell แล้วเลือกโฟลเดอร์บทนี้ (เครื่องอื่นให้เปลี่ยนส่วนหน้าของตำแหน่งตามที่วางหลักสูตร):

```powershell
Set-Location -LiteralPath 'C:\Users\kopes\Documents\Codex\2026-09-17\go-youtube-go-iot-edge-gateway\Go\lessons\phase-01\ep10-conditions'
go run ./examples
```

ผลที่คาดหวัง:

```text
WARNING
```

ถ้าผลต่าง ให้ตรวจตำแหน่งด้วย `Get-Location` ตรวจไฟล์ที่บันทึก และอ่าน error แรกก่อนแก้

## ทำไมโค้ดจึงทำงานแบบนี้

- Go ไม่ต้องใส่วงเล็บรอบเงื่อนไข แต่ต้องมี { } ครอบคำสั่ง
- เครื่องหมาย >= รวมค่าที่เท่ากับด้วย ถ้าใช้ > ค่า 30 จะไปอีกทาง
- ทำงานเพียงทางเดียวใน if/else ชุดนี้; เกณฑ์ 30 เป็นข้อกำหนดตัวอย่าง ไม่ใช่กฎของภาษา

**แยกประเภท:** syntax และชนิดข้อมูลเป็นหลักของภาษา; การแยกหน้าที่ให้ส่วนเล็กอ่านง่ายเป็นแนวทางออกแบบ; ชื่อโฟลเดอร์และการใช้หนึ่ง module ต่อ EP เป็นทางเลือกของหลักสูตร เกณฑ์อุณหภูมิที่พบเป็นข้อกำหนดจำลอง

## ลองแก้ในพื้นที่ฝึก

เปิด [วิธีสร้างพื้นที่ฝึก](../../../docs/PRACTICE.md) แล้วสร้าง EP.10 ด้วย `./scripts/new-practice.ps1 -Episode 10` จากราก Go เพียงครั้งเดียว จากนั้นเปิด `practics/phase-01/ep10-conditions` แก้ไฟล์ใน `examples` แล้วรัน `go run ./examples` จากโฟลเดอร์ EP ที่คัดลอกแล้ว

พื้นที่ฝึกไม่รวมเฉลย และคำสั่งสร้างจะหยุดถ้าโฟลเดอร์มีอยู่แล้ว จึงไม่ทับงานที่เคยทำ

## ข้อผิดพลาดที่พบบ่อย

- ใช้ = แทนการเปรียบเทียบ: = คือกำหนดค่า ส่วน == คือเท่ากัน
- วาง else แยกบรรทัดจาก } ก่อนหน้า: ใช้ } else { ตามตัวอย่าง

## แบบฝึกหัดและคำถามทบทวน

ทำ [แบบฝึกหัด 2 ข้อ](exercises/README.md) ก่อนดูเฉลย คาดผลของแต่ละข้อแล้วเปลี่ยนเพียงจุดที่โจทย์ระบุ

คำถามตรวจความเข้าใจ: ถ้าเปลี่ยน >= เป็น > แล้วใช้ค่า 30 ผลจะเปลี่ยนอย่างไร?

[เฉลยและเหตุผล](solutions/README.md) แยกไว้เพื่อเปิดหลังลองเอง ถ้ายังไม่ผ่าน ให้จดสิ่งที่คาดกับผลจริงไว้ก่อนขอความช่วยเหลือ

## สิ่งที่ได้และขอบเขต

เลือกการทำงานได้เอง ก่อนใช้กรองข้อมูลจำลองในบทต่อไป

ใช้ข้อมูลจำลองภายในโปรแกรมเท่านั้น ยังไม่ได้อ่านอุปกรณ์ ส่ง MQTT เรียก API หรือเก็บฐานข้อมูล

ผลตรวจจริงและส่วนที่ยังไม่ได้ทดสอบ: [tests/RESULTS.md](tests/RESULTS.md)

อ้างอิงหลัก: [เอกสาร Go ทางการ](https://go.dev/tour/flowcontrol/5)

เมื่ออธิบายผลและทำแบบฝึกหัดได้ ค่อยเลือก [ตอนถัดไป / สารบัญ](../ep11-loops/README.md) ไม่มีข้อกำหนดให้เรียนหลายตอนในวันเดียว
