# EP.7 — ค่าศูนย์เมื่อยังไม่กำหนดค่า

**เป้าหมาย:** คาดเดาค่าเริ่มต้นของตัวแปรและไม่ตีความว่าอ่านอุปกรณ์สำเร็จ

## 1. อ่านโค้ด

ตัวแปรที่ยังไม่ได้กำหนดค่าจะมีค่าศูนย์ของชนิดนั้น: ตัวเลขเป็น `0`, bool เป็น `false` และ string เป็น `""`

ไฟล์ [examples/main.go](examples/main.go)

```go
package main

import "fmt"

func main() {
	var count int
	var temperature float64
	var online bool
	var deviceName string
	fmt.Println(count)
	fmt.Println(temperature)
	fmt.Println(online)
	fmt.Println("[" + deviceName + "]")
}
```

## 2. ลองรัน

**ก่อนรัน:** ถ้าชื่อว่าง บรรทัดสุดท้ายจะหายไปหรือเห็น []?

รันจากโฟลเดอร์ `lessons/phase-01/ep07-zero-values`:

```shell
go run ./examples
```

<details>
<summary>ดูผลที่คาดหวังหลังลองรัน</summary>

```text
0
0
false
[]
```

ในตัวอย่าง `+` ใช้ต่อข้อความกับวงเล็บ เพื่อให้เห็นตำแหน่งของข้อความว่าง

</details>

<a id="practice"></a>

## 3. ฝึกเอง

ใช้ **`practics/main.go` ไฟล์เดิม** เปลี่ยนเนื้อหาทั้งไฟล์เป็นโค้ดจาก [ตัวอย่าง EP นี้](examples/main.go) แล้วทำโจทย์ด้านล่าง

ถ้ายังไม่มีไฟล์ ให้สร้างโฟลเดอร์ `practics` ใน `Go` แล้วสร้าง `main.go` ข้างใน ไม่ต้องมี `examples` หรือ `go.mod` ดู [วิธีสร้างครั้งแรก](../../../docs/PRACTICE.md)

**แก้ `main.go` ในโฟลเดอร์ฝึก** ทีละข้อ:

1. ประกาศชื่อว่าง พิมพ์ใน [] แล้วกำหนด sensor-01 และพิมพ์อีกครั้ง
2. ประกาศ online แบบ bool ไม่ใส่ค่า พิมพ์แล้วเปลี่ยนเป็น true พิมพ์อีกครั้ง

บันทึกไฟล์ (Ctrl+S) แล้วรันจาก **`practics`**:

```shell
go run main.go
```

<details>
<summary>คำถามทบทวนหลังทำโจทย์</summary>

เลขศูนย์อย่างเดียวบอกได้ไหมว่าอ่านอุปกรณ์มาแล้ว?

[ดูเฉลยหลังลองทำ](solutions/README.md)

</details>

<details>
<summary>อ่านเพิ่มเติมเมื่อสงสัย</summary>

Go ให้ค่าเริ่มต้นที่แน่นอนเมื่อประกาศด้วย var โดยไม่ใส่ค่า แต่เลข 0 ในตัวแปรไม่ได้ยืนยันว่าอุปกรณ์วัดได้ 0 จริง ต้องแยกความหมายของข้อมูลกับค่าเริ่มต้น

**zero value** คือค่าที่ได้เมื่อยังไม่กำหนดค่า: int/float64 เป็น 0, bool เป็น false, string เป็นข้อความว่าง

- var แบบไม่มี = ยังมีค่าให้ใช้ ไม่ใช่ค่าขยะ
- เครื่องหมาย + เมื่อใช้กับ string คือการต่อข้อความ ใส่วงเล็บเหลี่ยมเพื่อให้เห็นตำแหน่งข้อความว่าง
- ค่า false เป็นค่าเริ่มต้น ไม่ได้เกิดจากการตรวจการเชื่อมต่อในตัวอย่างนี้

**ข้อผิดพลาดที่พบบ่อย**

- คิดว่า 0 ยืนยันค่าเซนเซอร์: ตัวอย่างไม่ได้อ่านอุปกรณ์เลย
- ดูข้อความว่างไม่ออก: ครอบด้วยตัวคั่นหรือใช้ %q ในตอนจัดรูปแบบ

[เฉลย](solutions/README.md) · [ผลตรวจและข้อจำกัด](tests/RESULTS.md) · [เอกสาร Go](https://go.dev/tour/basics/12)

</details>

[ตอนก่อนหน้า](../ep06-data-types/README.md) · [สารบัญ](../../../docs/playlist-01-go-basic/README.md) · [ตอนถัดไป](../ep08-format-output/README.md)
