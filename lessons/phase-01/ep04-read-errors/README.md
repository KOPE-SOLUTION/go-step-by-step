# EP.4 — อ่านข้อผิดพลาดจากการสะกดชื่อ

Phase 1 — Go Basic • [สารบัญ](../../../docs/playlist-01-go-basic/README.md)

## เป้าหมายและสิ่งที่ควรรู้ก่อน

แยกการหาเครื่องมือไม่พบจาก compile error และแก้ทีละจุด ก่อนเริ่มควรทำ EP.3 ได้และอธิบายตัวอย่างด้วยคำของตนเอง ไม่จำเป็นต้องจำโค้ดจากความจำ

บทนี้มีหนึ่งเรื่องหลัก อ่านเป็นช่วงได้: ทำความเข้าใจ → คาดเดา → รัน → เปลี่ยนทีละจุด ถ้าต้องกลับไปทบทวนใช้ [ตอนก่อนหน้า](../ep03-output-lines/README.md)

## แนวคิดและคำใหม่

compiler เหมือนคนตรวจคำสั่งก่อนเริ่มงาน ถ้าพบชื่อที่ใช้ไม่ได้ โปรแกรมยังไม่ถูกสั่งทำงาน ให้เริ่มอ่านชื่อไฟล์และเลขบรรทัดของ error ก่อนเดาว่าเครื่องมือเสีย

**compile** คือการแปลงและตรวจ source ก่อนรัน; **error** คือข้อความแจ้งปัญหา; เลขหลังชื่อไฟล์มักบอกบรรทัดและคอลัมน์

## หยุดคาดเดาก่อนเปิดผล

ถ้าเขียน fmt.println โปรแกรมจะพิมพ์ก่อนแล้วค่อยแจ้ง error หรือยังไม่เริ่มทำงาน?

จดคำตอบสั้น ๆ ก่อนรัน ผิดได้ เก็บเหตุผลที่คิดไว้เทียบกับสิ่งที่เกิดขึ้น

## ตัวอย่างเล็กที่รันได้

### examples/main.go

```go
package main

import "fmt"

func main() {
	fmt.Println("Check complete")
}
```

## รันและตรวจผล

เปิด PowerShell แล้วเลือกโฟลเดอร์บทนี้ (เครื่องอื่นให้เปลี่ยนส่วนหน้าของตำแหน่งตามที่วางหลักสูตร):

```powershell
Set-Location -LiteralPath 'C:\Users\kopes\Documents\Codex\2026-09-17\go-youtube-go-iot-edge-gateway\Go\lessons\phase-01\ep04-read-errors'
go run ./examples
```

ผลที่คาดหวัง:

```text
Check complete
```

ถ้าผลต่าง ให้ตรวจตำแหน่งด้วย `Get-Location` ตรวจไฟล์ที่บันทึก และอ่าน error แรกก่อนแก้

## ทำไมโค้ดจึงทำงานแบบนี้

- ตัวอย่างที่ให้เป็นโค้ดถูกต้อง เก็บไว้เทียบกับงานฝึก
- ในพื้นที่ฝึกเปลี่ยนเป็น `fmt.println` เพียงจุดเดียว บันทึกแล้วใช้คำสั่งรันเดิม
- มองหา `undefined: fmt.println` และเทียบกับ `Println` ก่อนแก้กลับ; ข้อความต่อท้ายอาจต่างตามรุ่น

**แยกประเภท:** syntax และชนิดข้อมูลเป็นหลักของภาษา; การแยกหน้าที่ให้ส่วนเล็กอ่านง่ายเป็นแนวทางออกแบบ; ชื่อโฟลเดอร์และการใช้หนึ่ง module ต่อ EP เป็นทางเลือกของหลักสูตร เกณฑ์อุณหภูมิที่พบเป็นข้อกำหนดจำลอง

## ลองแก้ในพื้นที่ฝึก

เปิด [วิธีสร้างพื้นที่ฝึก](../../../docs/PRACTICE.md) แล้วสร้าง EP.4 ด้วย `./scripts/new-practice.ps1 -Episode 4` จากราก Go เพียงครั้งเดียว จากนั้นเปิด `practics/phase-01/ep04-read-errors` แก้ไฟล์ใน `examples` แล้วรัน `go run ./examples` จากโฟลเดอร์ EP ที่คัดลอกแล้ว

พื้นที่ฝึกไม่รวมเฉลย และคำสั่งสร้างจะหยุดถ้าโฟลเดอร์มีอยู่แล้ว จึงไม่ทับงานที่เคยทำ

## ข้อผิดพลาดที่พบบ่อย

- เปลี่ยนหลายจุดก่อนรัน: ทำให้แยกสาเหตุยาก ให้กลับมาเทียบทีละบรรทัด
- แก้ชื่อแต่ยังไม่บันทึก: compiler จะอ่านไฟล์ที่บันทึกอยู่

## แบบฝึกหัดและคำถามทบทวน

ทำ [แบบฝึกหัด 2 ข้อ](exercises/README.md) ก่อนดูเฉลย คาดผลของแต่ละข้อแล้วเปลี่ยนเพียงจุดที่โจทย์ระบุ

คำถามตรวจความเข้าใจ: go ไม่เป็นที่รู้จัก กับ undefined: fmt.println ต่างกันที่ขั้นไหน?

[เฉลยและเหตุผล](solutions/README.md) แยกไว้เพื่อเปิดหลังลองเอง ถ้ายังไม่ผ่าน ให้จดสิ่งที่คาดกับผลจริงไว้ก่อนขอความช่วยเหลือ

## สิ่งที่ได้และขอบเขต

ใช้หลักฐานจาก error แทนการคัดลอกโค้ดทับทั้งหมด

ใช้ข้อมูลจำลองภายในโปรแกรมเท่านั้น ยังไม่ได้อ่านอุปกรณ์ ส่ง MQTT เรียก API หรือเก็บฐานข้อมูล

ผลตรวจจริงและส่วนที่ยังไม่ได้ทดสอบ: [tests/RESULTS.md](tests/RESULTS.md)

อ้างอิงหลัก: [เอกสาร Go ทางการ](https://go.dev/doc/tutorial/getting-started)

เมื่ออธิบายผลและทำแบบฝึกหัดได้ ค่อยเลือก [ตอนถัดไป / สารบัญ](../ep05-variables/README.md) ไม่มีข้อกำหนดให้เรียนหลายตอนในวันเดียว
